根据您的要求，我将实施“条款输入与展示”功能。

### 实施计划

1.  **添加条款输入框 (UI)**
    *   **位置**：在 `index.html` 的翻译表单区域，紧跟在行李输入行下方。
    *   **组件**：添加一个新的 `<textarea id="terms">`。
    *   **默认值**：
        ```text
        机票条款：
        可退 全程未飞前 需扣除退票费400欧 其余款可退
        可改 改签费200欧+舱位差价（需现查报价为准）
        有效期6个月。
        ```
    *   **样式**：高度自适应（默认约 4 行），支持换行，全宽显示。

2.  **更新可视化卡片 (Visual Card)**
    *   **HTML**：在 `#visualCard` 的底部（Footer 上方）添加一个新的 `div` 区域用于显示条款。
    *   **JS**：修改 `renderVisualCard` 函数，读取输入框中的内容，并将其渲染到卡片的条款区域。
    *   **样式**：使用 10pt+ 字体，1.2倍行距，左右边距 15px，保持整洁。

3.  **更新文本生成 (Text Result)**
    *   **JS**：修改 `generateResultText` 函数，在生成的纯文本结果末尾也追加这些条款内容，确保复制文本时包含条款。

### 执行步骤
1.  **修改 `index.html`**：插入输入框 HTML，更新 CSS，修改 JS 渲染逻辑。
2.  **推送**：提交代码到 Git。
