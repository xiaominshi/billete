收到，我将立即着手修复**并发安全问题**。

### 🛠️ 修复方案：实现请求隔离与状态分离

为了彻底解决多用户并发导致的数据混淆问题，同时保证性能，我将执行以下修改：

#### 1. 修改 `logic.py`
- **优化初始化**: 修改 `Logic.__init__` 方法，允许传入现有的 `airport_map`。这样在创建新实例时，就不需要每次都重新读取数据库，实现轻量级实例化。
- **性能微调**: 将 `import pytz` 从循环内部移至文件顶部，减少重复导入开销。

#### 2. 修改 `server.py`
- **移除全局单例**: 删除全局的 `logic = Logic()`。
- **引入共享状态**: 在应用启动时加载 `GLOBAL_AIRPORT_MAP`。
- **请求隔离**: 在 `/process` 路由中，为每个请求创建一个新的 `Logic(airport_map=GLOBAL_AIRPORT_MAP)` 实例。这样每个请求都有独立的 `passengers` 和 `flights` 状态，互不干扰。
- **同步更新**: 确保在添加/删除机场时，同步更新全局的 `GLOBAL_AIRPORT_MAP`。

这一方案能以最小的代码改动，彻底消除并发隐患，并保持高性能。确认后立即执行。