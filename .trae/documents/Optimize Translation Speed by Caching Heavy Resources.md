为了大幅提升翻译速度并满足您的要求（不依赖网络、仅使用本地数据、未匹配不翻译），我将执行以下优化：

### 实施计划

1.  **修改 `logic.py` (实现全局缓存)**：
    *   创建一个全局变量 `_GLOBAL_AIRPORT_CACHE` 来存储机场数据。
    *   新增一个 `init_global_cache()` 函数：负责一次性加载所有机场数据（包括 `airportsdata` 库和本地数据库中的自定义机场）。
    *   **改造 `Logic` 类的初始化 (`__init__`)**：
        *   不再每次都重新加载文件和查询数据库。
        *   直接引用已加载好的全局缓存。
        *   这将使每次点击“翻译”时的后台处理时间从几秒缩短到几毫秒。

2.  **修改 `server.py` (启动时加载)**：
    *   在服务器启动代码中直接调用 `logic.init_global_cache()`。
    *   确保在您打开网页之前，所有数据都已经准备好放在内存里了。

### 预期效果
*   **零网络依赖**：翻译完全依赖本地内存数据。
*   **极速响应**：消除了每次请求的文件读取开销，翻译将几乎瞬间完成。
