**现状说明**
- 代码已支持使用 .env 中的 DATABASE_URL 连接 Neon（PostgreSQL），并启用了适配 Neon 的连接参数（pool_pre_ping、NullPool）。

**快速核验**
1. 打开 http://127.0.0.1:5000/db/health
- 预期返回：{"dialect":"postgresql","ok":true}
2. 执行一次翻译（点击“开始翻译”）后，在 Neon 控制台查看 history 表新增记录
- 说明本地写入已落到同一个云库

**若未连接（修复步骤）**
1. 创建/更新 .env：
- DATABASE_URL=postgresql://neondb_owner:你的密码@ep-flat-mountain-ago04r9m-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
2. 安装驱动：
- pip install psycopg2-binary
3. 重新启动服务：
- python billetepython/server.py
4. 再次访问 /db/health 与 Neon 控制台验证

**可选增强**
- 在 /version 返回当前数据库 URL 的主机名（隐藏凭证）用于快速辨识连接目标
- 将历史保留周期改为可配置，避免数据自动清理导致验证困难

确认后我将按以上步骤核验本地连接，并在同一库下完成一次写入验证与报告。