您要求**新建一个独立的表格**来专门存储这些导入的成本和售价数据，而不是直接覆盖历史记录。

### 实现方案

我将创建一个名为 **`financials`** 的新表，用于存储从文本导入的财务数据。

**1. 数据库层 (`database.py`)**

*   **新增表结构 `financials`**：
    *   `id`: 主键
    *   `ticket_number`: 票号 (唯一键，用于去重/覆盖)
    *   `loc`: 订单号 (LOC)
    *   `passenger`: 乘客姓名
    *   `cost`: 实际买价
    *   `price`: 卖价
    *   `profit`: 利润
    *   `issue_date`: 出票日期
    *   `user_id`: 关联操作用户 (可选)
    *   `timestamp`: 导入时间
*   **新增函数 `upsert_financial_record(ticket_number, data)`**：
    *   使用 `INSERT OR REPLACE` (SQLite) 或 `ON CONFLICT DO UPDATE` 逻辑，确保同一票号的数据会被覆盖。

**2. 后端逻辑 (`server.py`)**

*   **新增路由 `/import/financials` (POST)**：
    *   接收文本数据。
    *   解析制表符分隔的列。
    *   提取字段：
        *   Col 0: 出票日期
        *   Col 1: LOC
        *   Col 5: 票号 (Key)
        *   Col 7: 乘客 (WE)
        *   Col 8: 成本
        *   Col 10: 卖价
        *   Col 11: 利润
    *   调用数据库函数批量插入/更新。
*   **新增路由 `/financials` (GET)**：
    *   用于前端获取已导入的财务列表，展示给用户。

**3. 前端界面 (`templates/index.html`)**

*   **新增 "💰 财务管理" 卡片**：
    *   包含 **“📥 导入数据”** 按钮。
    *   包含一个表格，展示已导入的财务数据（票号、乘客、成本、售价、利润）。
*   **导入弹窗**：
    *   大文本框供粘贴。
    *   确认按钮。

**4. 关联逻辑 (可选)**

*   虽然数据存储在独立表格，但我可以在展示时尝试通过 `LOC` 或 `票号` 简单匹配历史记录中的行程信息，增强显示效果（如果需要）。目前主要专注于存储和展示导入的数据。

### 执行步骤

1.  **修改 `database.py`**：定义 `financials` 表并添加操作函数。
2.  **修改 `server.py`**：实现导入解析和查询接口。
3.  **修改 `templates/index.html`**：添加新的 UI 模块。
4.  **验证**：使用您的样本文本测试导入和覆盖逻辑。

是否确认执行？
