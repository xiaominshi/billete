您指出的问题非常关键。当前的解析逻辑确实是取了“前两个”时间，而实际数据中包含了一个“干扰时间”（通常是第一个），导致解析结果整体前移，错把起飞时间当成到达时间，或者把无效时间当成起飞时间。

**测试结果确认：**
我编写的测试脚本 `test_fix.py` 运行结果显示 **全部失败**，这证实了当前逻辑（即使跳过了航站楼）仍然错误地提取了第一个时间。

* **错误现象**：提取了 `17:05 -> 17:50`

* **正确预期**：应提取 `17:50 -> 20:35`

**修复方案：**

我将修改 `logic.py` 中的 `parse_flight` 方法，采取 **“取后两个”** 的策略：

1. 扫描一行中**所有**符合时间格式（`HHMM` 或 `HHMM+1`）的字段。
2. 如果找到 3 个或更多的时间字段，**忽略前面所有的干扰时间，只取最后两个**分别作为起飞和到达时间。
3. 如果只找到 2 个，则直接作为起飞和到达时间。

这将完美解决所有行的问题：

* 行1：`1705`, `1750`, `2035` -> 取 `1750` (起飞), `2035` (到达)

* 行2：`2220`, `2320`, `1735+1` -> 取 `2320` (起飞), `1735+1` (到达)

* ...以此类推。

**下一步：**

1. 修改代码实现上述逻辑。
2. 再次运行测试脚本验证。

