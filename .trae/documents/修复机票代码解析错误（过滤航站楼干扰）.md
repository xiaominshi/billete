您遇到了包含航站楼信息（如 `2E`, `2`, `2F`）导致代码解析出错的问题。这些非时间格式的字符被错误地识别为到达时间，导致系统计算时长时崩溃或产生错误数据。

**修复方案：**

我将修改 `logic.py` 中的航段解析逻辑。

1.  **智能时间提取**：
    *   不再简单地取“起飞时间”后面的一个字段作为“到达时间”。
    *   改为从起飞时间开始向后扫描，提取**前两个**符合时间格式（`HHMM` 或 `HHMM+1`）的字段。
    *   自动跳过任何不符合时间格式的干扰项（如航站楼代码）。

2.  **预期效果**：
    *   对于 `2220 2E 2320`，系统将自动跳过 `2E`，正确识别 `2220` 为起飞，`2320` 为到达。
    *   对于没有干扰的正常数据（如 `1705 1750`），解析逻辑保持不变。
    *   彻底解决 `invalid literal for int()` 这类报错。

**验证计划：**
我已编写了一个复现脚本，确认了当前代码会报错。修复后，我将再次运行该脚本，确保解析结果正确无误。
