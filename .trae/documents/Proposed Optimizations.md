好的，我将为您同时实现 **PDF 导出** 和 **多语言切换** 功能。

## 实施计划

### 1. 📄 PDF 导出功能 (PDF Export)
- **引入库**: 在 `index.html` 中引入 `jspdf` 库 (配合已有的 `html2canvas`)。
- **UI 变更**: 在“生成图片”按钮旁添加 **"📄 下载 PDF"** 按钮。
- **逻辑实现**:
  - 使用 `html2canvas` 捕获高清的 `#visualCard`。
  - 使用 `jspdf` 将截图放入 A4 大小的 PDF 文档中并自动下载。

### 2. 🌍 多语言切换 (Bilingual Toggle)
- **UI 变更**: 在页面顶部添加 **"中文 / English"** 切换开关。
- **数据结构**: 创建一个包含所有界面文案的字典 (Dictionary)，涵盖标题、标签、按钮、提示语等。
- **逻辑实现**:
  - 点击开关时，遍历 DOM 元素并更新文本内容。
  - 将用户的语言偏好保存到本地 (localStorage)，下次访问自动应用。

我将立即开始修改 `index.html` 来实现这两个功能。