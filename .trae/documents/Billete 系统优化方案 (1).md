# 优化文本结果格式

根据您的具体要求，我将对生成的翻译文本进行以下调整：

## 1. 修改“停留时间”为“转机时间”
- **操作**：更新多语言配置。
- **细节**：将 "停留时间" (Layover at) 改为 "转机时间" (Transfer time)，并且**不再显示地点名称**（如“天府”），只显示时长。
    - 中文：`转机时间: 3小时35分`
    - 英文：`Transfer time: 3h 35m`
    - 西班牙文：`Tiempo de escala: 3h 35m`

## 2. 调整时间显示位置
- **操作**：修改 `generateResultText` 函数逻辑。
- **细节**：将转机时间的显示顺序从“两个航班之间”移动到“第二段航班之后”。
    - **修改前**：
      ```
      航班 A
      停留时间
      航班 B
      ```
    - **修改后**：
      ```
      航班 A
      航班 B
      转机时间
      ```

这将完全匹配您提供的样例格式。

## 3. (可选) 视觉卡片 (Visual Card) 更新
- 我也会同步更新生成的图片/PDF中的标签，将其中的“停留时间”也改为“转机时间”，保持术语一致。但图片中的**位置**暂保持在两段航班之间（因为这是时间轴视图，符合视觉逻辑），如果您也希望调整图片里的位置，请告知。

准备执行以上修改。